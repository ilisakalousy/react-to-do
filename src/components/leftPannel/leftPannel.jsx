import React, { useState } from "react";

import { StyledLeftPannel } from "./styles";

function LeftPannel({ isActive }) {
    return (
        <StyledLeftPannel isActive={isActive}>
        
        </StyledLeftPannel>
    );
}

export default LeftPannel;